import { Elements } from "@kontent-ai/delivery-sdk";

export default interface BackgroundImageComponentProps {
    Url: string,
    Orientation: string,
    Content: JSX.Element,
    Container: boolean
}